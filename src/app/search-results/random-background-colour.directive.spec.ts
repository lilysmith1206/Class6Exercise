import { RandomBackgroundColourDirective } from './random-background-colour.directive';

describe('RandomBackgroundColourDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomBackgroundColourDirective();
    expect(directive).toBeTruthy();
  });
});
